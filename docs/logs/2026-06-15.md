# 2026-06-15 — Nhận & niêm phong "nâng cấp ở nhà": rebrand **Canvas M** + Glass-Desk login/admin

> Anh Luân làm ở nhà rồi push lên `feature/canvas-m-login-admin-ui`. Phiên này: phân định phần nào đã trên master vs phần mới, lập **team 6 agent** review + chạy 3 cổng kỹ thuật để đảm bảo "không vỡ", bịt 2 lỗ rebrand còn sót, rồi **ff-merge vào master** (ahead 2, **chưa push** — chờ anh review).

## 1. Phân định phần "ở nhà"

Branch đi thẳng từ master, lệch đúng **1 commit gốc**:

- `997000cf "update giao dien dashboard"` — **đã nằm trên master** từ 06-12: bản triển khai design-system **Glass Desk** cho dashboard (xem §2, spec đầy đủ trong `plans/glass-desk-dashboard-2026.md`).
- `6572d821 "MCM UI: Canvas M rebrand + Glass-Desk login & admin redesign"` — **phần MỚI** trên feature branch (xem §3).

Vì lineage thẳng nên merge là **fast-forward sạch**, không conflict.

## 2. Đã có trên master từ 06-12 — Glass Desk dashboard (tóm tắt)

Triển khai theo `plans/glass-desk-dashboard-2026.md` (authoritative — đọc file đó cho spec token/vật liệu/bất biến). Khảo sát file xác nhận các mảng đã land:

- **Token Glass-Desk** trong `MeetingShell.scss` (scope `.mcm-lobby`): kính frosted-mirror (blur 22px, sat/bright, sàn đục .52|.47), concentric radii, elevation ring-trong-shadow, kind-colors My Files, motion vars.
- **Wallpaper** (`data/wallpaper.ts` + `WallpaperPicker.tsx` + `Wallpaper.scss`): đổi hình nền desk sau lớp kính — preset (forest-mist, crystal-leaves) + gradient (olive-champagne, pastel-day) + **upload riêng** (resize ≤1920 / JPEG q0.8 client-side trước khi vào localStorage; QuotaExceeded → báo, không apply nửa vời). Per-browser (localStorage `mcm:wallpaper`), **không sync account** — thuần thẩm mỹ; đọc lúc module-init nên paint đúng từ frame đầu.
- **CalendarX** (`data/calendar.ts` + `CalendarX.tsx`): lịch + **ghi chú per-day / per-meeting** qua route worker mới **`GET/PUT /v1/notes`** (scope `day|meeting`, loud-quiet — lỗi mạng trả "" / false, không crash lịch).
- **Color/Icon** (`ColorMenu.tsx` = `ColorMenu` + `EmojiMenu`, migration **`0018_color_icon.sql`**): `project.color/icon` + `meeting.icon` (bổ sung `meeting.color` của 0011). Đều **COSMETIC** → worker miễn guard organizer-only / finished-immutable. Emoji preset tách bộ project (toà nhà/công trình) vs meeting (hoạt động).

## 3. Commit ở nhà (`6572d821`) — rebrand + login/admin

**a. Rebrand "MAP CanvasMeet" → "Canvas M"** + logo ảnh `public/canvas-m.png` (117KB) thay text/glyph:

- `AppWelcomeScreen` Center.Logo, `AdminConsole` header (`<img>` thay span "MAP") + `org_name` default, `MeetingLobby` header (`<img>` thay glyph ◳ + text).
- i18n hero subtitle vi/en/ko ("Canvas M: một canvas chung…").

**b. LoginScreen redesign** (`LoginScreen.tsx` +154):

- **Intro cinematic**: wordmark hiện giữa màn → tan → bàn giao card (timeout `INTRO_MS=2400`, **bỏ qua hẳn khi `prefers-reduced-motion`** — không bẫy user, timeout luôn resolve + cleanup on unmount).
- **Luồng login bằng LINK cuộc họp (MỚI)**: mode thứ 3 `"link"` cạnh password/magic. Dán URL collab / `#room=` / `ID,KEY` → `parseRoomLink` regex (mirror parser của MeetingLobby) → ghi `window.location.hash = #room=ID,KEY` → vẫn **bắt đăng nhập** (password hoặc magic-link) → MeetingLobby auto-join sau khi có session. Login chỉ _mang theo ý định_, không bypass auth. 6 key i18n mới `link*` (parity đủ vi/en/ko).

**c. AdminConsole redesign** (`AdminConsole.scss` +329): Glass-Desk, wordmark mờ chìm góc dưới-phải (`/canvas-m.png` watermark, pure decor).

**d. `MeetingShell.scss` +576**: token + style cho login pane + admin glass + brand wordmark.

## 4. Kiểm tra — 3 cổng + team 6 agent (đảm bảo "k broke")

**Cổng kỹ thuật (chạy trên feature branch):**

- ✅ `yarn test:typecheck` (tsc) — pass.
- ✅ `yarn eslint` 8 file đổi — **0 error** (202 warning prettier thuần-format; code mới chưa chạy qua prettier — xem §6).
- ✅ `yarn build` full — **pass 52s**, SCSS login/admin compile sạch (chỉ warning chunk-size/browserslist vô hại). Đây là cổng thật cho 576+329 dòng SCSS mà typecheck/eslint không đụng tới.

**Team review (workflow `wf_96fbbc55-996`, 4 mảng song song → verify đối nghịch):**

- `login-flow`, `admin`, `scss-scope`: **sạch** — luồng login-link wiring đúng, không regression password/magic/guest; admin không class chết / ref vỡ; SCSS +576 **scope an toàn, không rò sang canvas họp**, blur budget ≤4 giữ, dark-override đủ ngữ cảnh.
- `rebrand-sweep`: ra **2 gap** (đều rebrand-chưa-trọn, KHÔNG phải vỡ) → xem §5.

## 5. Bịt 2 lỗ rebrand + merge

Team tìm ra rebrand sót 2 chỗ user-visible, đã fix (commit `d254f695`):

1. **`index.html`** (medium) — branch không đụng file này: `<title>`, meta name/description, OG (site_name/title/image:alt/description), `<h1>` ẩn còn "MAP CanvasMeet" → tab trình duyệt + preview chia sẻ link (Slack/Teams/OG) vẫn tên cũ. **Sửa 8 chỗ → "Canvas M".**
2. **`MeetingHeader.tsx:408`** (low, pre-existing ngoài scope) — header trong phòng họp còn `<strong>MAP CANVAS MEET</strong>`. Đổi → `Canvas M` (giữ `(MCM)` vì là code-name nội bộ, prefix class `mcm-` vẫn dùng khắp nơi).

Còn lại "MAP CanvasMeet"/"Map Canvas Meet" chỉ trong **comment / acronym expansion** (MeetingLobby.tsx:36, MeetingShell.tsx/scss) → để nguyên (không user-visible).

**Merge:** `git merge --ff-only` → master `997000cf..d254f695`. Master hiện **ahead origin/master 2 commit**, cây làm việc sạch. **Chưa push** — chờ anh review.

## CÒN LẠI — lưu ý / làm tiếp

- [ ] **Push origin/master** khi anh duyệt (2 commit: `6572d821` home + `d254f695` fix rebrand).
- [ ] **Prettier**: code mới (login/admin) 202 warning format — `yarn eslint --fix` cho 8 file để dọn, nếu muốn diff sạch chuẩn repo.
- [ ] **Test tay luồng login-link** 2 account (dán link khi chưa đăng nhập → login password/magic → auto-join đúng phòng) — chưa chạy live.
- [ ] (Mang sang từ 06-11) cutover client lên remote + Pages, khoá CORS/password/rate-limit, recording→R2, election validate server-side.
