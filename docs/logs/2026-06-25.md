# 2026-06-25 — mobile mic fix · end-meeting teardown · UNIFIED REPLAY · review-correctness

**Prod cuối ngày:** Pages `6130f85e` · Worker `466b11a6` · D1 0038.

## ✅ Design-sync panel canvas → Glass Desk (Pages `6130f85e`)
Audit (1 Explore): đa số panel/floating ĐÃ Glass Desk (replay dock/lanes, STT, call-controls, people-bar); video well + caption giữ tối là đúng. Lệch chính = **Meeting Library** + **Chat** vì sống trong scope `.excalidraw` (kế thừa token Excalidraw/`--chat-*` cục bộ), và dark theme shell = `#121212` neutral ≠ Olive-Royal dashboard.
- **Stamp lobby tokens** lên `.excalidraw .MeetingLibrary`/`.ChatView` (light + dark) — cũng là thứ cấp `--mcm-surface-0`/radius/shadow mà panel cần (shell base không có). `MeetingShell.scss` (Lead).
- **MeetingLibrary.scss**: swap token Excalidraw (`--island-bg`/`--text-primary`/`--default-border`/`--color-primary`) → `--mcm-*`; giữ scrim tối trên thumbnail + màu file-kind. (agent)
- **ChatPanel.scss**: remap 10 biến `--chat-*` → `--mcm-*` ở root `.ChatView`; giữ màu AI-bot tím riêng. (agent)

## ✅ Batch review-chrome + projfiles + đếm file (Pages `e3a8db7d`)
- **Ẩn Layout/Caption(CC)/Present ở review** — đều là control LIVE; review không có call/màn để present. MEDIA group + separator gộp gate `!viewOnly`; CC + LayoutSwitcher gate riêng. `MeetingHeader.tsx`.
- **Thêm CHỮ vào nút Replay + Meeting log** — `--labeled` + `mcm-header__icon-label` (review header thoáng nên hiện chữ; <1340px tự thu về icon). `MeetingHeader.tsx`.
- **Toggle Meetings|Files nhảy khi đổi tab** — toolbar(view/sort/status)+New-meeting chỉ hiện ở tab Meetings → mount/unmount quanh toggle làm nó xê. Fix: toggle `order:3 + flex:0 0 auto` (luôn cuối hàng, mép phải ghim) → control kia hiện/ẩn BÊN TRÁI, toggle đứng yên pixel-perfect. `MeetingShell.scss`.
- **Library họp đếm "All 6" mà chỉ 3** — `MeetingLibrary` lấy `meetingFilesAtom` gồm cả file nội bộ ẩn (`ifc-snap`/`pdf-snap`/`dxf-snap`/`mcm-deco`); per-kind + grid lọc nhưng "All" dùng raw `items.length`. Fix: 1 memo `visibleItems` (lọc internal + dedup theo id) cho mọi chỗ dùng. `MeetingLibrary.tsx`.

## ✅ Review-correctness pass (`88a2195b`, Pages `1c1f1b96`)
Team điều tra read-only (4 mảng) → Lead synthesize + giao 1 agent ParticipantsBar (file-disjoint), Lead tự làm SCSS + MeetingHeader.
- **Timing ở review CHẠY HOÀI** — header elapsed clock đếm `now - start` mãi vì `setInterval` không gate `meetingViewOnlyAtom`. Audit mọi timer (17 chỗ): đây là timer LIVE duy nhất chưa gate (heartbeat đã gate sẵn). Fix: dừng ticker + **ẩn** stat khi review (thời gian họp xong nằm ở replay span, không phải clock chạy). `MeetingHeader.tsx`.
- **Participants ở review** — trước reuse live socket presence (rỗng/cũ). Giờ là **roster điểm danh**: "Đã tham gia" (`listParticipants` joined_at) + "Đã mời" (invitee không dự); tắt control live dư thừa (knock/admit, mute/kick, in-call count, waiting count). Live y nguyên. `ParticipantsBar.tsx`.
- **Projects folder lạc quẻ (tối)** — `.mcm-folder` in-canvas kế thừa token tối của `.mcm-shell`. Stamp Glass Desk tokens (như `.mcm-admin`/`.mcm-login`) + scrim theme-aware. `MeetingShell.scss`.
- **Meeting-log modal nhảy giữa 3 tab** — panel center auto-size theo content. Fix: `height: min(720px,86vh)` cố định, chỉ `&__body` cuộn. `MeetingShell.scss`.

## ✅ Batch UI/UX 7 việc (Pages `931eb3bc`)
Team điều tra read-only 3 cụm → Lead giữ `MeetingShell.scss`, giao 4 agent file-disjoint (MeetingHeader/CanvasReplay.scss/UserSettings.scss/ScreenShareSelfView).
- **#3 recap player rò sang họp LIVE khác** (bug) — `replayOpen` là local state, không reset khi đổi phòng; dock portal body render chỉ gate `{replayOpen}`. Fix: `useEffect` reset khi `activeRoomLink`/`viewOnly` đổi + siết render `{replayOpen && viewOnly && roomId}`. `MeetingHeader.tsx`.
- **#7 review header hiện thời gian họp** — thay vì ẩn timer, review hiện nhãn TĨNH "DD/MM HH:MM → HH:MM" (start từ createdAt, end từ log cuối) khớp Span ở bottom. i18n `header.meetingTimeTitle` (vi/en/ko). `MeetingHeader.tsx`.
- **#1 tabs `mcm-projfiles__tabs` đè chữ** — base `.mcm-segmented__btn` cố định 28px (cho icon); tab này là chữ → tràn/đè. Override `width:auto; padding:0 12px; nowrap; @include t-btn`. `MeetingShell.scss`.
- **#2 header canvas lỗi responsive** — `.mcm-header__actions` `flex-shrink:0` ⇒ `overflow-x:auto` (820px) vô hiệu, nút phải (Leave/End) clip ngoài màn. Fix: rail `flex-shrink:1 + min-width:0` (scroll thật), con `>*` `flex-shrink:0` (không bóp), identity `flex-shrink:6` (nhường trước). `MeetingShell.scss`.
- **#6 nút exit replay chiếm 1 hàng** — `&__controls` `flex-wrap:wrap` + scrubber rộng đẩy X xuống. Fix: `nowrap + min-width:0 + overflow-x:auto`, scrubber `flex:1 1 auto/min-width:80px`, `--close` `flex:0 0 auto`. `CanvasReplay.scss`.
- **#5 account settings nhảy panel giữa tab** — card chỉ `max-height` ⇒ auto-size theo tab. Fix: `height: min(92vh,760px)` cố định + `__body{min-height:0;overflow-y:auto}`. `UserSettings.scss`.
- **#4 "You are presenting" cho floating** — `.mcm-ss-self` pin góc cố định. Thêm pointer-drag (inline left/top, clamp viewport, handle = bar, Stop chặn drag), không đụng SCSS. `ScreenShareSelfView.tsx`.

## ✅ Shipped + live
- **#25 mic iPhone** (`eb394afb`, Pages `68da3798`) — nút mic bị `disabled={!canTransmit}` = deadlock (chính nút đó là đường xin mic lần đầu, iOS cần user-gesture). Fix: nút luôn tap được khi đang trong call. `MeetingCallControls.tsx`. Owner xác nhận OK iPhone/iPad/PC.
- **#26/#27 end-for-all teardown** (`07774411`, Pages `b86694fd`) — End họp không teardown gì. (#27) `AudioRoomController` teardown Daily call khi `meetingViewOnlyAtom→true` (hết thấy/nghe user khác). (#26-A) `ParticipantMicRecorder` react `canTransmit` → thu được mic người unmute trễ. (#26-B) `CloudRecordingControls` auto-stop session khi vào review → flush+upload+nhả lock; `RecordingsSection` auto-refresh bắt upload trễ.
- **#28 UNIFIED REPLAY** (`999dd6f8`+`1dd2993e`, Pages `ce3f75d8`, migration 0038) — design-first qua team (3 bản design → tổng hợp `docs/plans/unified-replay-ux.md`), build 4 phase:
  - **P0** `started_at_ms`/track (stamp lúc `MediaRecorder.start()`) → đặt mọi track lên trục canvas.
  - **P1** player index→**clock ms tuyệt đối** (`ReplayClock`, rAF) — canvas-only y hệt cũ.
  - **P2** **SpeakerLanes "ai nói lúc nào"** từ transcript (không cần record), **xử lý nhiều-người**: ≤5 lanes; >5 → ribbon hội thoại tô màu + top-6 + "Người khác (k)" cuộn. `data/replayTimeline.ts` (test 12/12).
  - **P3** chooser **Canvas / Audio-along (trộn mọi mic + solo) / Screen-along (pane video nổi)**, sync nudge>250ms, memory bounded near-playhead, legacy null→fallback T0. `useReplayMedia.ts`.
  - Sửa luôn **#22** placeholder Recordings gọn lại (hết tràn).

## ⏳ Còn / next
- **#25b** iPhone vào call hơi chậm — verify lại; nghi STT auto-getUserMedia off-gesture (`AudioRoomController`).
- **#28 P4** (optional): zoom timeline, keyboard, hover-snippet — khi owner cần.

## Cách làm
Team: fan-out điều tra read-only → Lead synthesize + giao fix file-disjoint / tự fix → typecheck + build → deploy → log. Owner dặn: LUÔN lập team điều tra + làm việc.
