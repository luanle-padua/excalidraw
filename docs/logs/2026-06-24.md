# 2026-06-24 — dev-team day (big)

Standing dev-team set up + ran the backlog as an overseer loop (`/team`, charter `docs/handbook/dev-team.md`, backlog `docs/plans/TEAM-BACKLOG.md`). Many parallel sub-teams, file-disjoint, i18n merged centrally.

**Prod now:** Pages `map-canvasm` = `8ced77e5` · Worker `mcm-storage` = `685191a9` · D1 at 0036. **Toàn bộ batch 06-24 đã safety-commit vào git: `cba369b5`** (43 file; trước đó shipped-nhưng-uncommitted). (PWA giờ TỰ cập nhật khi đóng–mở app lại — xem mục deploy cuối bên dưới, hết cần unregister SW thủ công.)

## ✅ Shipped + live
- **#5 UI STATUS pills** — fixed twice: 1st bản sai scope (`.mcm-pmgr` vs `.mcm-pdetail`), 2nd chuyển rule top-level → hết đè. `ProjectManager.scss`.
- **#4 Recording no-mic + audio share** — surface Daily `screenAudio` qua `screenShareMediaAtom`, mix vào file; record được khi không mic (chỉ cần share màn). `screenShareState.ts`, `DailyScreenShare.ts`, `clientRecording.ts`, `CloudRecordingControls.tsx`.
- **#2+#3 Storage hard-delete** — admin xóa project = hard-delete R2 ngay (helper `deleteR2Prefix`; dọn `packages/*`, `project-files/*`, `guest-logos/*` + rows). Archive download = backup. `trash/` lifecycle = TODO dashboard. `worker/src/index.ts`, `wrangler.jsonc`.
- **#6 Consent/chat UX** — bỏ auto-open participants panel; chat panel mặc định mở; consent fade frosted trên canvas (giữ xác nhận).
- **#7 Chat unread badge** — pip tinh tế trên icon chat. `AppSidebar.*`, i18n `chat.unread`.
- **#12 Client REC indicator + late-join** — REC pill đỏ đặc/nhấp nháy + frame glow rõ; host re-broadcast RECORDING_STATE mỗi 5s (late-joiner thấy); gỡ Simple Analytics (hết lỗi ERR_BLOCKED_BY_CLIENT); status colors tint riêng từng trạng thái.
- **#8 Admit banner** — banner nổi giữa-trên cho host khi có người gõ cửa (Admit/Deny tại chỗ) + badge sáng. `KnockBanner.*`, `ParticipantsBar.tsx`.
- **#9 Canvas Replay** — rebuild native: drive canvas review qua `updateScene` (không Excalidraw thứ 2, không rebroadcast, scrub được, peek toggle); capture liên tục cả buổi (delta 3s, ≤1200 frame, E2E, 1 blob/buổi `canvas-history/<roomId>/current`); worker route `/v1/canvas-history/:roomId`. **Replay = nút "Tua lại" độc lập** ở review header.
- **#14 Recording logs UX** — Recordings = clip-list + player gọn; modal footer/subtitle tab-aware (bỏ "Download .md"/"0 segments" ở tab Recordings).
- **#13 Responsive overhaul (3 phase)** — P1 meeting view (touch 44px, header overflow, panel→drawer, iOS zoom), P2 dashboard (3-col stack, card grid 2/1-col, login), P3 admin (table scroll-in-card) + portal (stack) + modals (bottom-sheet/full-width phone). Additive media query 768/480.
- **#15 Bug-fix batch** — (A) record button 'frozen' = `setBusy(false)` sau upload chậm → chuyển trước upload nền; (B1) consent hiện mỗi lần = thêm localStorage cache per-device (`mcm-consent:roomId:version`) → re-entry tức thì, không flash; (B2) chat không bung lại khi vào lại = reset ref `didDefaultOpenChat` khi rời họp.
- **Pica** — lỗi đỏ console khi chèn ảnh (fingerprint protection chặn getImageData) → hạ thành warn (ảnh vẫn chèn). `packages/excalidraw/components/App.tsx`.

### 🔄 Sau khi viết log — deploy cuối `8ced77e5`
- **PWA tự cập nhật** — `public/_headers`: `sw.js` để `no-cache` (Cloudflare edge hết giữ SW cũ → browser luôn thấy bản mới); service worker `skipWaiting` + tự kiểm tra mỗi 1 phút / khi focus tab / khi có mạng → tự reload. Hết kẹt "phải clear cache / unregister SW": client chỉ cần đóng–mở app **1 lần** (cold start) để nuốt SW tự-update mới, sau đó tự động vĩnh viễn (hành vi 1-lần, như mọi PWA). `index.tsx`, `index.html`.
- **Recording — tiếng màn share** — Daily xin "share tab/system audio" → thu được tiếng của cửa sổ đang share vào file recording (nối tiếp #4).
- **Canvas Replay rỗng → fix** — bỏ guard chặn lưu lúc End (meeting flip view-only **trước** khi flush → save bị skip → replay rỗng); giờ flush canvas-history lên R2 khi rời/End. `saveCanvasHistoryNow` đã no-op khi recorder rỗng nên không ghi đè log thật bằng rỗng. `Collab.tsx` (nối tiếp #9).

### ✅ Batch #17–20 (committed `9147c9b6` — typecheck + build sạch — deployed Pages `b4c360a2`)
- **#17** consent hết nháy trên dashboard — root cause: `MeetingConsentGate` mount cả ở dashboard, chỉ gate theo `roomId` → fragment `#room=` còn sót làm nháy. Fix: gate thêm `isCollaboratingAtom` (chỉ hiện khi thực sự đang trong họp live). `MeetingConsentGate.tsx`.
- **#18** consent copy ấm/nhẹ/lợi-ích-trước (vẫn disclose recording + AI), EN/KO/VI. i18n `consent.*`.
- **#19** replay "Tua lại" → thanh điều khiển **dock đáy 1-click** (bỏ modal shell + bước peek; có nút thoát + Escape). `MeetingHeader.tsx`, `CanvasReplayPlayer/Timeline.tsx`, `CanvasReplay.scss`, key mới `replay.exit`.
- **#20** JOIN: gộp 5–6 lần `getMeeting(roomId)` lúc mở họp → 1 request (in-flight share + TTL 800ms, invalidate khi write; re-poll quan trọng — retry/END-verify/recheck 60s — dùng `{fresh:true}`) → nhanh ~1.5–4s. **Hoãn** parallelize socket+gate (rủi ro rò presence vào phòng bị cấm — cần thiết kế teardown riêng). `data/projects.ts`, `Collab.tsx`.

### ✅ Per-speaker recording + exclusivity lock (#23/#24) — deployed (Pages `2ef416aa` · Worker `f3e6e887` · D1 0037), commit `ab2a67a0`
Build production trọn gói (5-agent team: backend / client-recorder / UI / integration + Lead). Plan: `docs/plans/per-speaker-audio-recording.md`.
- **#24 độc quyền:** room DO giữ lock 1 chủ/phòng trong **socket-attachment** (sống qua hibernation, tự nhả khi close/ghost-reap). Client `recording-acquire`/`-release`; DO push `recording-state` (broadcast + unicast late-join). Hết cảnh 2 soft-host record chồng. Non-owner = chip xám "đang được ghi" (không Stop hộ người khác). `worker/src/roomDO.ts`.
- **#23 per-speaker:** mỗi participant ghi **mic của mình** (mono opus **32kbps**, audio-only) qua `ParticipantMicRecorder` (mount mọi người ở MeetingShell); chủ ghi thêm screen-audio (48k) + screen-video (VP8, opt-in). 1 `sessionId` gom các file. Worker upload gate mic = `canSeeMeeting` (mọi participant), speaker từ JWT; bảng `recording` thêm `kind/speaker_id/speaker_name/session_id` (migration 0037). UI Recordings gom theo phiên + người, player audio/video.
- **Tối ưu lưu trữ:** mic mono 32k, **skip-silent** (<600 B/s → không upload, không row rỗng), **không file mixed dư**.

### ✅ #25 — iPhone mic "off, không mở được" → FIXED (Pages `68da3798`, commit `eb394afb`)
Root cause = **deadlock UI**: nút mic `disabled={!canTransmit}`, mà `canTransmit = !!localStream`; nhưng chính nút đó (toggleMute → ensureMic) là đường **xin mic lần đầu** (popup quyền nổ trên user-gesture — iOS Safari BẮT BUỘC). Join luôn listener-only → không có mic → nút disable vĩnh viễn → không cách nào tap để xin quyền. iPad "chạy" vì đã lấy mic qua **STT auto-ensureMic**. Fix: nút mic luôn bấm được khi đang trong call (`onClick={toggleMute}`), title no-mic = "Bật micrô". `MeetingCallControls.tsx`.
- **Còn lại (#25b, CHƯA fix):** iPhone vào call "rất lâu rồi cũng vào" (chậm, không treo hẳn). Nghi `AudioRoomController` STT auto-`ensureMic` gọi getUserMedia **off-gesture** trên iOS → kẹt tới timeout. Verify sau khi test bản mic fix; nếu còn → gate STT-auto-acquire sau 1 gesture / thêm timeout join.

## ⏳ Đang chạy / còn
- **#16 Fix overlap + z-index** (audit xong): banner top-center đè (connection/knock/review), toast vs drawer z1200, caption vs people-bar phone, gallery vs presenter, + z-index scale. Team đang fix.
- **#10 Dọn worktree** (manual, bẫy junction Windows).
- TODO ops: R2 dashboard lifecycle expire `trash/` (cho guest-revoke soft-delete).
- Follow-up: shared breakpoint mixin; header overflow "More" menu (TSX); verify `me` lowercasing trong consent read (latent, không ảnh hưởng email thường).

## Cách làm (loop)
Lead/overseer dispatch team theo backlog → file-disjoint song song → review diff + typecheck + build (validate SCSS) → deploy Pages/Worker → ghi log. Writes serialize trên file chung (bẫy worktree Windows). Deploy chỉ khi cần test/owner yêu cầu.
