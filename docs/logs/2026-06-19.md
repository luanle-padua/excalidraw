# 2026-06-19 — STT THỰC SỰ CHẠY (gốc rễ `[object Blob]`) + đại tu caption + audit bảo mật tạo/join họp + User Settings & avatar SSOT + redesign header (logo thật)

> **Một ngày RẤT dài, 3 nhịp.** **Sáng:** chốt được **gốc rễ thật** của STT câm — Cloudflare giao frame nhị phân của client dưới dạng **Blob**, `providerWs.send(blob)` ép thành chuỗi `"[object Blob]"` gửi **text** cho Deepgram → Deepgram trả `{type:Error, variant:SchemaError}` mỗi frame → **STT câm KHẮP NƠI, MỌI provider**. Fix **1 dòng** (`binaryType=arraybuffer`) trị tất cả; rồi **đại tu hiển thị caption** (Caption Dock, `captionSurfaceAtom` 1 surface/view, Full/Compact, auto-scroll) + tối ưu audio (chống ồn, giảm latency) + audit design system. **Chiều:** ship một loạt lớn — **video quality 3 tầng**, **audit lỗ hổng tạo/join họp + fix bảo mật** (ghost presence, preview giả, ad-hoc ungated, hình/sticker khách không sync), **canvas authorship** (copy thành của người copy + chặn sửa text người khác), **User Settings 6-tab + avatar single-source-of-truth**, **join UX nhẹ** (listener-only, chat mở sẵn), **đổi nền/blur Daily**, **redesign HEADER** (dồn mọi call control lên header + **logo Canvas M thật**), **dọn sạch dấu vết Excalidraw + regenerate icon PWA từ logo thật**. Làm thẳng trên **`master`** (đã push). **State cuối ngày:** master = `2505f6d8`, app `https://map-canvasm.pages.dev` đã deploy bản mới nhất (nhiều vòng deploy trong ngày).

Cách làm giữ nguyên nếp: mọi thay đổi qua cổng typecheck (app + worker) trước build/deploy; debug bằng cách **ép lỗi lộ ra thật** (chrome://inspect trên iPad) rồi mới sửa, không đoán.

## 1. STT root cause `[object Blob]` — điểm nhấn của ngày (`61bc0b00`)

Đây là con bug đã ám STT suốt từ 06-17/06-18. Triệu chứng: AI nháy (Deepgram đã connect, handshake OK) nhưng **không ra một chữ nào**, trên **mọi thiết bị, mọi provider**. Hành trình loại trừ:

- **Loại trừ ngôn ngữ Deepgram** — đã chốt 06-18 nova-3 monolingual có `ko/vi/en`, `keyterm` chỉ gắn khi `ko`, bỏ `numerals`. Không phải đây.
- **Loại trừ clone track iOS** — 06-18 đã cho STT tap một `clone()` mic độc lập (tránh track bị Daily PeerConnection chiếm). Thêm log **PCM peak liên tục** (`8d0b05d4`, `f953b2cf`) để tách "clone im lặng" với "upstream từ chối": PCM peak có giá trị thật → **mic CÓ vào, không phải clone câm**.
- **Loại trừ worklet không chảy** — `d75e7588`: worklet cần một đường tới `destination` nếu không PCM không flow; `537cf12d` thêm indicator "đang thu" lái bằng **PCM thật**. Xác nhận PCM thật sự được đẩy lên Worker.
- **Loại trừ lệch ngôn ngữ** — log cả `lang` kèm peak, không phải sai param.
- **CHỐT bằng cách đọc raw Error** — `f953b2cf` log **mọi loại frame** server trả về; `566eace3` **dump nguyên payload Error của Deepgram** + xử lý cả frame `Error` viết hoa. Đọc qua **chrome://inspect** (iPad) ra đúng thủ phạm: `{type:Error, variant:SchemaError, message:"[object Blob]"}` **mỗi frame**.
- **Gốc rễ:** Workers runtime giao PCM của client dưới dạng **`Blob`**, không phải `ArrayBuffer`. `providerWs.send(blob)` **coerce Blob thành chuỗi `"[object Blob]"`** và gửi **text frame** cho Deepgram → Deepgram coi là control message hỏng → `SchemaError`. Audio **không bao giờ tới engine** → không có transcript. Cùng lỗ này làm ElevenLabs/OpenAI cũng chết (`pcmToBase64` throw trên Blob → drop frame).
- **Fix (`worker/src/stt.ts`, +10 dòng):** ép `ws.binaryType = "arraybuffer"` ở server WS để PCM forward đúng dạng nhị phân. **1 dòng trị mọi provider cùng lúc.** STT ra chữ thật.

> Bài học lặp lại: bug nằm ở **biên runtime** (Cloudflare WS giao Blob ≠ Node giao Buffer/ArrayBuffer), không ở logic ngôn ngữ. Chỉ vỡ ra khi **lộ payload lỗi thật** thay vì đọc `console.warn` chung chung.

## 2. Tối ưu audio — chống ồn + giảm latency (`33f78d48`)

- **Chống ồn:** box-filter anti-alias khi downsample PCM trong **worklet** (trước đây downsample thô → aliasing → nhiễu vào engine).
- **Giảm latency:** chunk **250 → 100ms** + bật **endpointing** của Deepgram → câu phiên âm ra nhanh & dứt câu gọn hơn, đỡ trễ.

## 3. Đại tu hiển thị transcription / caption

### 3a. Caption Dock (`e54cf608`)
Phụ đề LIVE riêng cho ngữ cảnh **present / screen-share**: **per-viewer** (mỗi người xem theo ngôn ngữ của mình), chọn được **font S/M/L**. Tách hẳn khỏi panel transcript đầy đủ.

### 3b. `captionSurfaceAtom` — selector trung tâm định tuyến caption (`e0ea5c42` + `7096c2bd`)
**Nguồn sự thật duy nhất** quyết định **đúng 1 surface** sở hữu caption cho từng view (`captionState.ts`), dập triệt để hiện-tượng caption double-mount / rò ra canvas trần. 4 case chốt:
- **Canvas trần** = **CHỈ panel STT** (không nhân bản dock thừa).
- **Gallery** (grid / speaker view) = **dock ghim ở ĐÁY**.
- **Viewer + Floating-Presenter** = gộp về **1 surface** → **giết duplicate FloatingPresenter** (trước đây caption hiện 2 chỗ).
- **Ngôn ngữ đúng viewer** = dịch caption theo đúng `sttTranslateEnabledAtom` mà panel dùng; `seg.lang='multi'` truyền `assumedSource=undefined` để backend **auto-detect** thay vì dịch sai nguồn.
- Thêm **toggle CC ở header** họp → bật/tắt caption ở **mọi view**, kể cả view mà dock không tự mount.

### 3c. Auto-scroll stick-to-bottom (`deccbef2`)
Transcript **tự cuộn xuống dòng mới nhất** + đồng bộ UI panel với chat (cùng nếp scroll/giao diện).

### 3d. Full / Compact toggle (`cd124eaf`, `21306db4`)
Panel transcript có 2 chế độ; **Compact = chỉ text** (bỏ chrome thừa). `21306db4` đồng bộ panel + caption theo **Glass-Desk** và **sửa dropdown chọn provider** trước đây không có style (lòi ra mặc định trình duyệt).

## 4. Share-window resilience (`9f96ec05`)

Refresh trang reset `screenShareStateAtom` mà **không ai re-broadcast** ai đang share → người xem refresh (hoặc vào muộn) **không subscribe** track Daily còn sống. Fix: người đang share **re-announce** qua `broadcastScreenShareSnapshot()` trong handler new-user (giống `broadcastUserProfileSnapshot`); `DailyScreenShare.reconcileLocalScreenShare()` tái khẳng định local khi rejoin Daily với track `screenVideo` còn active. Idempotent.
- **Giới hạn trình duyệt (không vá được):** chính **người share** mà tự reload thì browser **xé `getDisplayMedia`** → vẫn phải chọn lại cửa sổ. Người **xem** refresh thì giờ thấy lại share.

## 5. Đồng bộ UI + indicator + iPad debug (rải trong các commit STT)

- **Indicator "đang thu / không nghe thấy mic"** lái bằng **PCM thật** (`537cf12d`) — không còn đoán theo trạng thái session.
- **Host thấy guest knock** (`e8a39e10`): deps của poll phụ thuộc `viewerAuthority` thay vì `iAmHost` → host mới nhận được tiếng gõ cửa phòng chờ. (Là vá nối tiếp deadlock guest 06-18; bản cũ chưa kịp lên.)
- **Hành trình debug iPad** qua **chrome://inspect** là chìa khóa mở được mục 1 — không có nó thì không bao giờ đọc được payload `[object Blob]`.

## 6. Audit + chiến lược design system (`7dd1f407`)

Viết `docs/plans/design-system-unification.md`: chiến lược **đồng bộ design system** giữa 2 vùng token đang lệch — **dashboard (Glass-Desk) vs canvas/meeting**. Mục tiêu hợp nhất token để panel/caption/header không còn mỗi nơi một kiểu. Mới là **plan**, chưa thực thi.

---

> **TỪ ĐÂY LÀ NHỊP CHIỀU** (commit `1b460b18` 12:25 → `2505f6d8` 16:31). Sáng dập xong STT/caption thì chiều quay sang **audit bảo mật luồng tạo/join họp**, **video quality**, **User Settings + avatar**, và **redesign header**. Hai commit docs `22bb3122` (infrastructure spec + roadmap delta) và `49f52375` (regenerate architecture.md) là tài liệu, không đụng tới ở log này.

## 7. STT — Gemini Live provider (skeleton) + dọn log debug (`1b460b18`)

- **Wire `gemini-3.5-live-translate-preview`** vào A/B picker dưới dạng option **disabled "coming soon"** (worker `GeminiLiveAdapter` throw `"provider not configured"` cho tới khi có `GEMINI_LIVE_API_KEY` + protocol bidi-WS). Mới là **seam**, chưa chạy.
- **Dọn log debug** ồn ào thêm vào khi truy `[object Blob]` (rx dump, ready/first-transcript/PCM-flowing). **Giữ** phần chức năng: route `Error→onError`, cảnh báo cloned-track, heartbeat `peak→onCapture`.

## 8. Video quality 3 tầng (user pick + admin cap + Daily ABR) (`5e41c4cf`, `af09b598`, `8b1bc6a7`, `044890fc`)

- **Vấn đề:** camera bị ghim **360p** không có send-settings, kéo layer nhận thấp nhất → **video bệch**. `8b1bc6a7` nâng capture **720p@30 (ideal)** + send Daily `quality-optimized` simulcast + tile không-active kéo **layer giữa** (active speaker vẫn top).
- **3 tầng (`044890fc`, `videoQuality.ts` = nguồn sự thật):**
  - **Daily adaptive bitrate (ABR)** luôn bật (tự hạ khi uplink yếu).
  - **Trần per-user**: Auto / Low / Medium / High trong **User Settings**.
  - **Trần org-wide admin**: `system_settings.video_quality_cap` đặt trong **Admin Console**.
  - **Hiệu lực = `clamp(user, cap)`**, ABR float bên dưới. Cap **cưỡi `GET /v1/config`** xuống client. Low/Medium còn **hạ resolution capture** để cắt CPU + egress.
- **Đổi nền / blur (`af09b598` → dời vào Settings `5e41c4cf`):** control Background cạnh nút camera → **None / Blur (nhẹ/vừa/mạnh) / 3 ảnh preset (nền công ty)**, wire `updateInputSettings({video:{processor}})`. Lựa chọn **persist** + re-apply khi camera bật. **Desktop-only** (Daily processor không chạy mobile web — disable + tooltip trên touch). `5e41c4cf` dời control này từ call-controls vào **Settings > Preferences** cho gọn. `videoBg.ts`.

## 9. Audit lỗ hổng tạo/join họp + 4 fix bảo mật (`23d4289e`)

> Rà soát luồng **create/join + dữ liệu khách** lòi ra **4 vấn đề thật**, fix trong 1 commit:

- **Ghost presence (tile "Guest" ma):** snapshot `USER_PROFILE`/re-announce có thể **hồi sinh** một peer đã rời. Fix: `updateCollaborator` + `peerProfiles` giờ **gate qua `liveSocketIds`** — set dựng lại CHỈ từ roster `room-user-change` (nguồn uy quyền). Peer đã rời không resurrect được.
- **PREVIEW 4-user giả nhá khi join thật:** khi URL có `#room=` thì **ẩn** preview giả (hiện placeholder "Joining" đã i18n); header count khớp lại với bar (`meetingMock`).
- **Phòng ad-hoc ungated:** client giờ **await + retry `registerMeeting`** (không còn fire-and-forget im lặng); **worker fail-close** realtime join cho phòng chưa đăng ký (`handleRealtimeUpgrade`, có **audit log**), và **STT coi như không-phải-member**.
- **Hình/sticker khách không sync:** `FileManager` **retry các save R2 lỗi** (cap theo `fileId@version`) → ảnh của khách chưa-admit upload **được một lần sau khi admit**; **sticker** giờ đi qua pipeline **pending→R2** nên persist cho late-join/reopen.

## 10. Canvas authorship — copy thành của người copy + chặn sửa text người khác (`8bc0b36d`, `4414f142`, `f875c097`)

- **Re-author khi sửa text (`8bc0b36d`):** sửa nội dung một text element **đóng dấu lại `customData.mcmAuthor`** sang người sửa ("ai sửa thì sở hữu") qua prop mới `getTextEditAuthor` (resolve từ socketId + username); chỉ chạy khi **đổi nội dung thật**, bỏ qua text bot, **không chạy khi reload** → copy vẫn thuộc tác giả gốc cho tới khi thực sự bị sửa. Kèm: box **dịch chatbot wrap (`wrapText`) khớp bề rộng** câu trả lời (hết "dài thòng" 1 dòng).
- **Wire đúng + re-author khi copy (`4414f142`):** bản restamp cũ **no-op âm thầm** — `getTextEditAuthor` thêm vào types + đọc ở App nhưng **chưa forward** qua wrapper `ExcalidrawBase` (`index.tsx`) nên luôn `undefined`. Forward nó. Thêm hook `onDuplicate`: paste/alt-drag/duplicate một text → **restamp `mcmAuthor` sang người copy** (bản copy thành của họ, bản gốc nguyên vẹn). `newElementWith` bump version → broadcast cho peer + persist + refresh badge.
- **Chặn sửa text người khác (`f875c097`):** **không mở được** editor trên text của người khác (hay của bot) — toast "copy-only"; copy vẫn thành của mình. Thêm **email ổn định** vào `customData.mcmAuthor` để text của mình **vẫn sửa được qua reload** (match email, fallback socketId cho legacy). Thay restamp cũ bằng **gate `canEditTextElement`** wire qua wrapper. Toast i18n vi/en/ko.

## 11. User Settings 6-tab + avatar single-source-of-truth (`2ad3ab44`, `67b926e5`)

- **Modal Settings Glass-Desk 6 tab (`2ad3ab44`)** mở từ gear họp + user-menu dashboard:
  - **Profile** — qua `ProfileEditor` tách ra từ `UserProfileModal` (name/company/avatar).
  - **Account/Org** — read-only từ `GET /v1/me` mới (email/title/division/role) thay vì fan-out cả directory.
  - **Security** — đổi password **verify-old-first** (re-auth `signInWithPassword` rồi `updateUser`); ẩn với guest; sign out.
  - **Preferences** — gom các atom pref rải rác (language, theme, translate, STT, caption, panel density).
  - **My Data / Privacy**.
- **Avatar SSOT:** một `resolveAvatarUrl` xử lý cả ref gallery `lib:` lẫn ref upload R2; avatar data-URL upload giờ **PUT lên R2** (`PUT /v1/me/avatar`) + **roam qua Supabase `user_metadata`**; `deriveSession` nhận ref R2 nên **load được trên máy khác**; **icon collaborator trên canvas resolve cùng nguồn** với mọi bề mặt avatar khác.
- **Gộp Profile + dọn (`67b926e5`):** sửa profile/avatar giờ **chỉ** ở Settings > Profile — **bỏ `UserProfileModal` trùng** (click avatar-mình + user-menu đều mở Settings với `initialTab`). **Trim gallery avatar 105 → 10** (resolver giữ nguyên nên pick cũ vẫn render). User mới **mặc định theme TỐI** (pick đã lưu vẫn thắng). Language vi/en/ko đã đủ trong Settings.

## 12. Join UX nhẹ — listener-only, chat mở sẵn (`ca1521a6`)

- **Join thẳng listener-only:** vào họp **không xin mic up-front** (`audioSource:false`, **không popup permission**) — nghe mọi người ngay; **mic acquire on-demand** lần đầu unmute (hoặc bật STT) qua `DailyAudio.ensureMic()` **idempotent**. Nút idle giờ chỉ là **"Join"** (icon cửa), camera vẫn opt-in.
- **Chat panel auto-open mỗi room session** (ref-guard, vẫn đóng được) thay vì chỉ once-ever-per-browser.

## 13. Redesign HEADER — dồn mọi call control lên header + logo Canvas M thật (`cb1ef35d`, `2505f6d8`)

- **Dồn mọi action (`cb1ef35d`)** (mic, camera, present, raise-hand, reactions, recording, leave, captions, transcript, layout, invite, settings, end) vào **một header icon có tooltip hover**, **bỏ thanh control nổi đáy** (giữ ParticipantsBar). **Trái:** **logo Canvas M thật** (`canvas-m.png`, thay chữ "M" CSS) + tên họp + timer + đếm người. **Phải:** **5 nhóm** phân cách hairline (Media / Interaction / Meeting / Chrome / Exit). Style **mượn chrome lobby dashboard** (glass surface, hover wash, group separator, state tint) → họp và dashboard đọc như **một sản phẩm**. Wiring/handler/atom **không đổi**. Responsive: action rail ưu tiên, identity capsule co trước.
- **Chuẩn hoá icon lucide (`2505f6d8`):** thay SVG inline tự chế + emoji bằng **lucide-react** một size (18) một stroke (2.0). Sửa **đụng độ icon exit**: **leave-call = `PhoneOff`** (cúp máy), **leave-meeting = `LogOut`** (rời phòng), **end-for-all = `Power`** (host, đỏ). Present dùng `ScreenShare`, idle join dùng `DoorOpen`. Recording giữ pill chấm-đỏ. State màu (muted/recording/end) giữ.

## 14. Brand / PWA — xoá dấu vết Excalidraw + icon thật (`10736382`, `3acaddc7`)

- **Dọn dấu vết Excalidraw (`10736382`):** thêm icon 512 + maskable vào manifest (install icon sắc, không crop); **xoá 11 asset marketing Excalidraw cũ** còn reachable qua URL (og-image, screenshots, oss promo); **gỡ link brand Excalidraw** khỏi Help dialog + bỏ upsell "Excalidraw+" trong message AI rate-limit.
- **Regenerate icon PWA từ logo thật (`3acaddc7`):** crop chữ **M serif** từ `public/canvas-m.png` (wordmark brand thật ở màn login) ghép thành install icon vuông trên nền **cream `#f5f3ef`** — thay M generic placeholder. Phủ android-chrome 192/512, apple-touch, favicon 16/32, maskable 192/512 (M đặt vào safe-zone adaptive). **Icon home-screen/install giờ là mark Canvas M thật.**

## TRẠNG THÁI CUỐI NGÀY

- **master = `2505f6d8`** (đã push origin). Dải commit hôm nay: `d75e7588 … 2505f6d8` (06-19), nối tiếp `e8a39e10`. Nhịp sáng (STT/caption) `d75e7588…7dd1f407`; nhịp chiều `1b460b18…2505f6d8` (12:25→16:31).
- **App Cloudflare Pages** `https://map-canvasm.pages.dev` — deploy **bản mới nhất**, qua **nhiều vòng deploy** trong ngày.
- **STT ĐÃ ra chữ thật** trên thiết bị thực (sau fix `[object Blob]`).
- **Audit bảo mật tạo/join họp đã đóng 4 lỗ** (ghost presence, preview giả, ad-hoc ungated fail-closed, hình/sticker khách sync).
- **User Settings 6-tab + avatar SSOT** (R2 + roam qua `user_metadata`, icon canvas cùng nguồn) đã land.
- **Header redesign** dồn mọi control + **logo Canvas M thật**; dấu vết Excalidraw + icon PWA đã thay bằng brand thật.
- _(Docs `22bb3122` infrastructure spec + `49f52375` regenerate architecture.md — của ngày, không thuộc log này.)_

## CÒN NỢ / VIỆC SAU

1. **Option Gemini Live** cho STT — **skeleton đã land** (`1b460b18`, option disabled "coming soon"); còn cần secret `GEMINI_LIVE_API_KEY` + protocol bidi-WS thì mới chạy.
2. **Thực thi chiến lược design-system** theo `docs/plans/design-system-unification.md` (mới có plan, chưa hợp nhất token dashboard↔canvas).
3. **2 quyết định P3 còn treo:**
   - **Accent hue** (chốt màu nhấn chung cho design system hợp nhất).
   - **Typography** (chốt thang chữ / font chung dashboard↔canvas).
