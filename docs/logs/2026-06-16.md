# 2026-06-16 — Phân quyền "chức vụ ≠ vai trò", Phòng chờ knock-to-join, Trang khách Glass-Desk

> Phiên dài, nhiều việc, **đã push** (`origin/master` `3534164a..3d0a7da5`, 32 commit). Dùng nhiều "team" (workflow nhiều agent) để thiết kế/điều tra trước khi code. Mọi thay đổi qua cổng: app+worker typecheck, eslint, full build.

## 1. Phân quyền — REVISED 06-16 (đảo lại vài điểm 06-15)

Anh Luân chốt qua nhiều vòng hỏi-đáp (xem [[project_mcm-permission-model]] memory):

- **Division admin = CHỈ HEAD** (rank cao nhất phòng). **Bỏ deputy** khỏi mọi authz — đảo lại luật 06-15 "deputy = ngang head". Deputy/trưởng-phòng (vd 장도진 실장) giờ chỉ có quyền **qua vai trò dự án được gán**. Drop `deputy_email` khỏi 7 query worker (`projectAccess`, `canManageProject`, `isProjectLeadership`, `isDivisionHeadOfProject`, `isMeetingProjectAuthority`, GET /projects is_head + lead-arm). Cột `deputy_email` để dormant.
- **Chức vụ (직급, từ user.csv) ≠ vai trò dự án** (leader/co-operator/participant). Chức vụ chỉ HIỂN THỊ (chip xám trung tính), KHÔNG khoá nút assign — ai cũng (kể cả head) assign được leader/co-operator. Server không hề gate theo title → chỉ là việc client.
- **Tạo dự án = mọi nội bộ** (đảo "head/deputy-only"): người tạo auto leader+owner, không trao quyền xuyên dự án.
- **Re-dịch `roleDivisionAdmin`** vi/ko (đang là "Trưởng phòng ban/부서장" = chức vụ → đổi thành tầng quyền "Quản trị phòng ban/부서 관리자").
- Commit `88231a8b` (meeting-manager-only invite/revoke, nối tiếp WIP hôm trước) + `855f3b1e` (split + head-only) + `363b2263` (fix modal assign-leader: nút save tàng hình do portal mất token scope + `{{email}}`→`{email}`).

## 2. Roster gộp 1 list (commit `8cf63c79`)

Trước đây member hiện **2 lần** (lưới chip PeopleGrid + danh sách role phẳng) → gộp thành **1 list nhóm theo phòng**, mỗi người 1 dòng đầy đủ: avatar · tên · chip 직급 · badge vai trò · nút (Assign leader / Make co-operator / × xoá). Bỏ lưới chip.

## 3. Bug form lịch gắn nhầm dự án — 1 gốc, 3 triệu chứng (commit `2f30666d`)

Team điều tra (high confidence): form **lên lịch** mở từ calendar không set `selectedProject` → `targetProject` fallback `projects.find(canManageProj)` = dự án-quản-được ĐẦU TIÊN (thứ tự server tuỳ ý). Hệ quả **cùng gốc**:

- Guest mới "mất khỏi list" (picker query nhầm dự án).
- **Admin phòng khác START được** họp phòng này (họp bị tạo dưới dự án của họ → họ là authority).
- **Admin phòng khác JOIN được** (canSeeMeeting nhận authority của dự án-bị-gắn-nhầm).

Fix: form lịch có **selector dự án** (không fallback bừa); `projectId=""` → bắt chọn; guest-picker re-fetch theo dự án chọn; sort dự án ổn định. Guest KHÔNG mất — luôn ghi đúng DB.

## 4. Siết quyền START + auto-join (đúng phòng sở hữu)

- **Auto-join họp = chỉ leadership** (`canSeeMeeting` member-arm siết về role owner/manager): member thường **phải được mời thủ công** mỗi họp ("đó là bảo mật"). Head/leader mở được qua nhánh authority (confidential vẫn chỉ invitee/owner).
- **START (scheduled→live) = chỉ PHÒNG SỞ HỮU** (commit `2ed69a67`): trước đây gate chỉ `isInternalEmail` (acting-host = bất kỳ nội bộ) → admin phòng khác start được. Thêm `isOwningDeptMember` (division của caller == lead_division của dự án) + organizer/host/cohost/authority. GET /v1/meetings trả `viewer_can_start` để client ẩn nút Start với người ngoài phòng. ("dự án phòng này làm chủ, phòng khác không nhảy vô start được").

## 5. Phòng chờ (Waiting room) — Phase 4 knock-to-join (commit `37e5953c`, `3e13f1c2`)

Thiết kế + spec + security-review qua team. **Khách ngoài** gõ cửa → chờ; **nội bộ @mapgroup auto-admit**; host **Cho vào / Từ chối**.

- Worker: migration **`0025_meeting_knock`** + 4 route (POST /knock external-only · GET /knock self-poll · GET /knocks host queue `isMeetingManager` · PATCH /knock/:email admit/deny soft + cooldown 30s server-enforced). **Cổng duyệt ép ở Daily token** (decision **1a**): external chưa admitted → không lấy token → không audio. End-gate giữ nguyên (authority).
- Client: `WaitingRoom.tsx` (clone WaitingForStart, poll 5s, re-read status khi admit để tránh race phòng-đã-finished, vào **muted**); nhánh park trong `startCollaboration`; mục "Waiting (knocking)" + Admit/Deny trong ParticipantsBar + badge "N waiting" trên CountChip.
- Fix Rules-of-Hooks: poll host bị đặt dưới early-return → hoist lên trên (`3e13f1c2`).
- **Bug 403 guest** (`95942b6c`): gate kiểm tra knock bằng tên room Daily `…-audio` thay vì meeting id gốc → strip `-audio` (kèm bịt lỗ canSeeMeeting bị bypass bởi hậu tố).

## 6. Trang khách (ClientPortal) Glass-Desk — đa quốc gia

- **Redesign Glass-Desk** (`785e2b43`): card kính mờ + nền full-bleed.
- **Nền xoay đa quốc gia** (`8569ff70`): component `PortalBackdrop` data-driven (thêm theme = thêm 1 dòng + thả ảnh), **lazy-load** từng theme. Fix **lộ panel khi crossfade** bằng **nền đục** (opaque base) dưới các lớp ảnh. Dùng chung cho cả phòng chờ (`.mcm-gate--client`).
- **Calendar trong suốt** nổi bên phải (`ClientCalendar`, đơn giản — chấm ngày có họp + nav tháng; không như app chính).
- **Brand**: bỏ logo CANVAS M khỏi panel → watermark trên nền; panel ghi **"MAP-GROUP Global"** (`6959a58d`). Greeting đưa **ra ngoài nền**, canh trái, font **Cormorant Garamond**, dòng **"Hi {name}" là hero to nhất** (`1db06a27`, `b6626840`). Phòng chờ đồng bộ font/kính.
- **Nén ảnh** (`3d0a7da5`): 7 nền PNG → **WebP** (≤1920w, q74) — **15.3 MB → 741 KB** (~95%). Panel chờ trong suốt hơn (surface 56% + blur 26px).

## 7. Chi nhánh VN + dev fixes

- **Overseas Subsidiary Vietnam Corporation** (`ee4e7608`): division mới + 3 user Supabase (이정호 부사장=head · 장현석 소장(S1) · 안 4급사원) + D1 division/user_division (local). 김진아 **chưa tạo** (email `김진아@` non-ASCII Supabase từ chối — cần email ASCII); Kim Won-jik bỏ (Executive Director không khớp 1 직급). Quick-login VN (`6d4cc008`), bỏ crown badge "quê" (`b7790d07`).
- **Fresh-test wipe** local (`d31209ed` + chạy): xoá project/meeting, GIỮ users/divisions/user_file/clients.
- **Dev crash fix** (`ded25e2d`): `excalidraw-app/.eslintignore` (build/dist) — vite-plugin-checker lint build output → ENOENT.

## CÒN LẠI — lưu ý / làm tiếp

- [ ] **Migration `0025_meeting_knock` CHƯA chạy remote** — apply khi deploy worker (`migrate.mjs --remote`).
- [ ] **Canvas relay vẫn trust-the-key** (1a): ai có `#room=ID,KEY` vẫn đọc/vẽ canvas (lỗi sẵn có, không phải do waiting room). Cổng audio + blob ĐÃ chặn. **1b = auth room server → blocker TRƯỚC khi mở cho khách ngoài thật** (xem `plans/waiting-room.md`).
- [ ] **김진아** chờ email ASCII để tạo nốt (소장 S2).
- [ ] **Theming khách**: hiện 7 nền xoay chung mọi khách. Bản "theme theo từng client/insight" (strategy team đã bàn) = làm sau, gắn `?rt=` hoặc per-client khi cần.
- [ ] Mang sang: test tay live (guest knock→admit→audio; member ngoài phòng không start được; cross-dept không join); cutover remote + Phase 6 hardening.
