# 2026-06-17 — Durable Objects realtime BUILT + DEPLOYED LIVE, app LÊN Cloudflare Pages, 100% DO

> Ngày lớn. **Đã push** (`origin/master` `47f66579..165547b8`, 18 commit). Dùng nhiều "team" (workflow nhiều agent) chạy song song 10 slice + audit + consolidation. Mọi thay đổi qua cổng: app+worker typecheck, eslint, full build. **Kết quả cuối ngày: toàn bộ stack ĐÃ ONLINE trên Cloudflare** — app tại https://map-canvasm.pages.dev, realtime mặc định Durable Objects, không còn máy dev / room server / Fly.

## 1. Durable Objects migration — BUILT + DEPLOYED LIVE (100% DO)

Sáng nay mới **viết kế hoạch** ([durable-objects-migration.md](../plans/durable-objects-migration.md), commit `68fde99e`), chiều đã **build xong + deploy LIVE**. Plan parity-safe, nuốt luôn 1b/B12.

- **Foundation** (`09d7851a`): class `RoomDO` trên Worker `mcm-storage` — **1 DO = 1 phòng** (`env.ROOM.idFromName(roomId)` deterministic), **WebSocket Hibernation API** (idle → $0 compute), `serializeAttachment({socketId,email,role,joinedAt})` sống qua hibernate, `setWebSocketAutoResponse` ping/pong không wake. Dual-run sau cờ `realtime_backend` (do|socketio) — runtime **per-meeting từ D1**, KHÔNG build-time (chống split-brain cross-build).
- **Build song song 10 slice** (qua nhiều team):
  - **Relay** — `server-broadcast` (OPEN only) + `server-volatile-broadcast` (check `bufferedAmount` backpressure) + `request-room-clients` + `rtc-signal`/`rtc-error` (kept-but-unused, forward-compat Daily đã thay mesh).
  - **Auth handshake (đóng 1b/B12)** — verify Ở WORKER **trước khi trả 101**: Supabase JWT (subprotocol token, echo `mcm.v1` không echo JWT) → `canSeeMeeting` → knock `admitted` (external) → WS-count cap. FAIL = 401/403, **không spin up DO**. Đây là lỗ relay-không-verify mà socket.io không bao giờ vá được.
  - **Presence invariant §3.1** — `first-in-room` suy từ cờ `roomEverInitialized` trong `ctx.storage` (KHÔNG `getWebSockets().length` → wake-from-hibernate KHÔNG clear scene người reconnect); debounce `room-user-change` ~250ms (gộp N×close lúc deploy); reconnect KHÔNG `Portal.close()` (giữ `broadcastedElementVersions`, tránh bão full-scene).
  - **Client `RawWsTransport`** — mimic bề mặt socket.io (`.on/.off/.emit/.id/.close`), swap DUY NHẤT tại `Collab.tsx:1100-1126`; frame phân biệt string (control) vs ArrayBuffer (scene E2E); reconnect backoff+jitter; encrypt/decrypt + 19 `WS_SUBTYPES` byte-identical.
  - **Cờ runtime `realtime_backend`** per-meeting đọc end-to-end (absent/null → socketio non-breaking).
- **Audit hoàn chỉnh** (parity 100%): rà cả 19 WS_SUBTYPES + control-frame → **không bỏ rơi feature nào** (scene/presence/follow/rtc/lock/chat/STT/recording/host-command/library-file). Checklist parity Team C nằm trong plan.
- **Consolidation pass** bắt **2 bug tích hợp thật**: (1) subprotocol token — server echo nhầm; (2) init-room socketId — client đọc `args[0].socketId` set `.id`. Fix trước khi cắt.
- **Admin "Realtime" monitor tab** (`36831dce`): xem live rooms / connections / backend đang dùng / handshake reject audit (DO từ chối ai, lý do gì).
- **DEPLOY LIVE** (`f3631fb1`): deploy lên Worker `mcm-storage`; **meeting mới default `realtime_backend='do'`**. Realtime chạy DO ngay trên Worker, không host riêng.
- **CHỐT lớn: 100% DO** (`c25a929e`, plan REVISED) — **bỏ ý tưởng Fly.io bridge, khai tử room server socket.io**. Lý do: dự án đi **đa quốc gia** (sắp có khách **Phi/Africa**) → serverless all-in Cloudflare, không SPOF Node 1-instance. Cờ `realtime_backend` chỉ là công tắc TẠM trong lúc build/test; đích = pure DO + bỏ hẳn `room/`.

## 2. I-1: AI/STT routes dời khỏi room/ lên Worker

- **`8e734411`** — `/translate`, `/translate-batch`, `/chatbot`, `/summarize` (Gemini) thành `ai.ts` trên Worker; `/stt` (Deepgram) thành `stt.ts` WS proxy **route riêng** (Worker switch theo `pathname` TRƯỚC `.get()` DO — `/stt` không bao giờ chạm RoomDO). Cache + rate-limit thành **per-isolate** (Gemini rẻ, đủ cho internal — P4 không over-engineer).
- **`9c72569b`** — sau đó **JWT-gate** cả AI + STT routes (đóng lỗ cost-abuse: trước đây public).

## 3. LIBRARY_FILE → R2-by-reference (cap 1 MiB)

- **`a85b1f64`** — Workers WS cap **1 MiB/msg** (socket.io cũ `maxHttpBufferSize 50MB` nhồi file inline). Đổi library-file broadcast sang **R2-by-reference**: chỉ gửi `{fileId, r2Key, meta}`, peer fetch R2; cap broadcast 256KB. Hard-break BẮT BUỘC trước cutover — đã fix.

## 4. Ops / go-live

- **Migration remote** (B2): áp **0017–0027** lên remote D1 — **27/27** schema_version khớp (waiting-room knock `0025` + usage `0026` + realtime_backend `0027` đều có trên remote, không còn knock-500).
- **Backup + DR** (B9, `8f637542` + `9274eb51`): **export D1 thủ công + restore-test PASS** (backup chưa test = backup giả → đã test thật). R2 **KHÔNG có S3 versioning** → soft-delete blob sang prefix `trash/` thay vì xoá vĩnh viễn (Bucket Locks không hợp bucket overwrite-nhiều — xem mục còn-lại). Secrets đã set.
- **B10 — DEPLOY app Cloudflare Pages**: app LIVE tại **https://map-canvasm.pages.dev**. **Toàn bộ stack giờ online trên Cloudflare** — không còn phụ thuộc máy dev / room server / Fly. PWA manifest rebrand **Excalidraw → Canvas M** (`4fe6f99f`).

## 5. Debug production sau go-live (app thật bắn token thật → lộ lỗi remote chưa từng gặp)

- **(a) `SUPABASE_URL` secret méo** (`57fbf58c`): secret dính `\n`/trailing-slash → `new URL()` throw "Invalid URL string" **NGOÀI** try → **500 trên MỌI route `/v1` có auth** (remote trước đó không có traffic-có-token nên lỗi này ngủ). Fix: trim + strip trailing slash; build JWKS URL trong guard trả **503 (misconfig rõ ràng) thay vì 500** uncaught. Hardened `jwtGate`.
- **(b) UTF-8 BOM trong secrets** (`b9046cfe`): `SUPABASE_SERVICE_API_KEY`, `DAILY_API_KEY`, `DAILY_DOMAIN`, `RESEND_API_KEY` set qua **PowerShell** dính BOM (﻿) → poison header → **502 trên Supabase-admin + Daily** (khách, add-member directory, screen share). Re-set sạch + thêm `cleanSecret()` guard read-side (`.trim()` strip U+FEFF) để rotate-từ-nguồn-BOM tương lai không tái diễn.
- **(c) AI routes public** → thêm **JWT gate** (B-AI, đã gộp ở `9c72569b`).
- **(d) CORS** (`57fbf58c`): allow origin **private-LAN (RFC1918)** → local dev server (qua LAN) gọi Worker online **y hệt production**.
- **(e) End-for-all delay** (`165547b8`): handler END_MEETING của non-host verify status bằng **1 lần** `getMeeting()` → trúng **stale D1 read-replica** trước khi write `status='finished'` lan → user kẹt trong phòng tới poll-60s. Fix: **retry verify ~5×/1s** → End thật flip review ~1-2s; broadcast giả vẫn verify not-finished rồi bỏ.
- **(f) Deco blob 404→204** (`165547b8`): `GET /v1/files/:roomId/:fileId` thiếu blob trả **204** (khớp scene/chat/transcript) thay vì 404; client `storage.ts` coi empty/204 = miss sạch (không decompress throw, không spam console, không thumbnail lỗi vĩnh viễn — deco file race throttled upload queue).
- **(g) Header count** (`165547b8`): chip người trong header bỏ chữ "people/người/명" → chỉ còn số (vd "3 · 🎙 2"); StickerPicker error→warn. Guest 403 waiting-room: điều tra → code ĐÚNG (403 = thật sự không-được-mời/đã-revoke/finished, đều intended), không sửa.

## CÒN LẠI — việc của anh Luân (trước khi mở external)

- [ ] **Bật Gemini "Generative Language API"** trong Google Cloud project **365448382193** — chừng nào chưa bật, translate/chatbot/summarize còn **502**.
- [ ] **ROTATE toàn bộ key** (B3): các key đã lộ (commit lịch sử) + lần này set không-BOM rồi → vẫn phải xoay TRƯỚC khi cho người ngoài.
- [ ] **R2 lifecycle rule** trên prefix `trash/` (R2 không có S3 versioning; Bucket Locks không hợp bucket overwrite-nhiều) — để tự dọn blob soft-delete.
- [ ] **Smoke test 2-client live** realtime DO (vẽ/presence/reconnect trên DO đã deploy).
- [ ] **DO slice-9 E2E/load** còn lại trên DO đã deploy (fanout full-scene 20s, eviction, hibernation 0-wake/10min).
