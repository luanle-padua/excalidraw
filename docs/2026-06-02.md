# 2026-06-02 — Tăng tốc mở lại cuộc họp (load-perf) + dọn data lỗi

## Mục tiêu phiên

Mở lại một cuộc họp đã lưu (cả **đang diễn ra** lẫn **đã xong**) hiện nội dung lên canvas **rất chậm** — dù scene đã nằm trong R2 và file đã có trong library. Lập **team agent** khảo sát toàn bộ path render để tìm gốc rễ, rồi sửa.

## Chẩn đoán — 3 lớp delay xếp chồng (team khảo sát)

1. **Scene (nét vẽ): chờ mù ~5 giây.** Thủ phạm chính. Lệnh tải scene từ R2 chỉ chạy *sau* khi socket bắt tay xong, qua sự kiện `first-in-room` hoặc khi peer gửi scene. Khi Leave rồi vào lại, socket cũ còn **ghost** trên room server (socket.io mặc định ~45s mới dọn) → server đếm room "chưa trống" → **không bắn `first-in-room`**, mà mình lại 1 mình nên không peer nào gửi → scene chỉ hiện sau **timeout dự phòng 5000ms** (`INITIAL_SCENE_UPDATE_TIMEOUT`).
2. **File/ảnh (IFC/DXF/PDF/ảnh):** `loadImageFiles` chạy sau scene render (trễ thêm); file 404 từ thời worker-down bị đánh dấu lỗi **vĩnh viễn** (`erroredFiles_fetch` làm `isFileTracked` = true → không fetch lại); peer fallback gửi dataURL nhiều MB **tuần tự** qua socket mã hoá → rất chậm cho IFC.
3. **Overlay MCM:** cache snapshot DXF/PDF/IFC nằm trong RAM, **reload là mất** → mỗi anchor DXF phải dựng lại WebGL renderer để bake lại PNG; overlay còn chờ hydrate thư viện (giải mã dataURL nhiều MB) mới chịu hiện.

## Đã sửa hôm nay (3 fix, typecheck pass)

1. **Eager parallel prefetch** — `Collab.tsx`: tải scene từ R2 **ngay lập tức, song song** với lúc socket connect; không chờ `first-in-room`/timeout 5s. Peer sống vẫn reconcile chồng lên sau (không set `socketInitialized` ở nhánh eager). Thêm cờ `eagerSceneLoaded` để `initializeRoom` bỏ qua re-fetch + resetScene (tránh nháy). → **Xoá hẳn 5s chờ** cho cả 2 kịch bản; file cũng bắt đầu tải sớm hơn ~5s theo.
2. **Reap ghost nhanh** — `room/src/index.ts`: `pingInterval 10s` + `pingTimeout 5s` (mặc định ~45s) → socket cũ rụng trong ~12s, `first-in-room` hoạt động đúng khi vào lại. **Cần restart room server :3002 để áp dụng.**
3. **Retry file tạm lỗi** — `storage.ts` `loadFilesFromStorage`: lỗi mạng/5xx (worker đang restart) thử lại 2 lần có backoff; **404 thật thì không retry** (khỏi phí thời gian với file chỉ có trên peer).

## Dọn data lỗi

- Xoá meeting rác **`room-test-1` ("Hop review mat dung")** trong D1: `room_key = "abc123key"` (9 ký tự, **không phải base64url hợp lệ**) → mọi lần mã hoá (cursor + save) ném `DataError: JWK member "k" could not be base64url decoded`. App vẫn cho vào vì key truthy → spam lỗi. Không có seed script tạo lại nên xoá là sạch.

---

## CÒN LẠI — MAI LÀM TIẾP

### A. Load-perf phase 2 (phần "full content" file/CAD còn chậm)
- [ ] **Persist snapshot cache DXF/PDF/IFC vào IndexedDB** + hiện thumbnail ngay → bỏ màn dựng lại WebGL mỗi lần reload. *(Win lớn nhất còn lại cho file CAD.)* — `dxfSnapshotCache.ts` (+ pdf/ifc siblings), `DXFCanvasOverlay.tsx` (thêm nhánh `<img>` thumbnail như PDF đã có).
- [ ] **File self-heal:** khi khôi phục bytes từ peer (`applyRemoteLibraryFile`) → re-PUT về R2 (`saveFilesToStorage`) để lần sau load nhanh + hết 404. Cho phép **retry file đã errored** (đừng để `erroredFiles_fetch` chặn vĩnh viễn — `FileManager.isFileTracked`, filter ở `Collab.tsx:617`).
- [ ] **Decode IFC GLB off main thread** (web worker) — focus anchor IFC còn block UI (`loadIfcMergedFromGlb` / `IFCRenderer`).
- [ ] **Tách library manifest** (id/name/mime/thumbnail) khỏi `dataURL` nặng → `known` true gần như tức thì, lazy-load bytes khi cần (`meetingLibrary.ts`).

### B. Review mode read-only (đã chốt với sếp, chưa code)
- [ ] Phân biệt **theo điểm vào**: vào từ **project folder tile** = **review read-only** (load tức thì, không edit nhầm) + nút **"Continue editing"** chuyển sang live; **Resume banner** = rejoin live (editable).
- [ ] Thêm `meetingViewModeAtom` + truyền `viewModeEnabled` prop vào `<Excalidraw>` (App.tsx) cho chắc; reset khi Leave.
- [ ] `startCollaboration(roomLinkData, { viewOnly })`; banner "Đang xem lại — Tiếp tục chỉnh sửa".

### C. Dọn nốt data demo
- [ ] 3 meeting rác `title=null, room_key=null` (`53ce1594…`, `8e5a2b49…`, `2e051c23…`) — không mở được, tile trống trong folder. **Chờ sếp xác nhận xoá.**
- [ ] Orphaned R2 scene `scenes/room-test-1/current` còn sót sau khi xoá meeting → xoá cho sạch bucket.

### D. Tồn đọng từ trước
- [ ] Tier 2 metadata (action items / decisions — list có cấu trúc).
- [ ] Deploy Cloudflare thật (hiện **local-only**), migrate realtime sang Durable Objects.

## Cần verify đầu phiên mai
- Restart **room server :3002** rồi test: mở lại cuộc họp in-progress & completed → nội dung phải hiện **< 1s**.
- Hard-reload tab (Collab là class component, HMR không nóng).
